kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g
# Android
android.useAndroidX=true
android.nonTransitiveRClass=true
# NOTE: the build needs a full JDK 17 (javac + jlink, for AGP's JdkImageTransform).
# The machine-specific toolchain path lives in ~/.gradle/gradle.properties (not committed)
# via org.gradle.java.installations.paths — see README. Keeps this file portable.
